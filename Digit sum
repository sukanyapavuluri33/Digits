#include<stdio.h>
void main(){
 int a,result;
 printf("input number:");
 scanf("%d",&a);
result=sum(a);
printf("sum of digits:%d",result);
         
}
int sum(int a){
    if(a!=0)
    {
        return (a%10+sum(a/10));
        
    }
    else{
        return 0;
    }
}
